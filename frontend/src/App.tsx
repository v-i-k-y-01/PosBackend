import { useEffect, useMemo, useState, type FormEvent, type ReactNode } from 'react';
import { BarChart3, Boxes, CheckCircle2, ChevronLeft, ChevronRight, ClipboardList, CreditCard, LayoutDashboard, LogOut, PackagePlus, Plus, ReceiptText, Search, ShoppingBag, Trash2, Users, WalletCards } from 'lucide-react';
import { ApiError } from './api/client';
import { posApi } from './api/pos';
import type { CartLine, Category, DailyRevenue, PagedResult, Product, Sale, TopProduct } from './api/types';
import { useAuth } from './auth/AuthContext';
import { EmptyState } from './components/EmptyState';
import { Modal } from './components/Modal';
import { Toast } from './components/Toast';

type View = 'dashboard' | 'checkout' | 'inventory' | 'categories' | 'sales' | 'team';
type Notice = { message: string; type?: 'success' | 'error' } | null;
const money = new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 2 });
const formatMoney = (value: number) => money.format(value);
const formatDate = (date: string) => new Intl.DateTimeFormat('en-IN', { day: 'numeric', month: 'short', hour: 'numeric', minute: '2-digit' }).format(new Date(date));

function LoginScreen() {
  const { login } = useAuth();
  const [mode, setMode] = useState<'login' | 'register'>('login');
  const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  const submit = async (event: FormEvent) => { event.preventDefault(); setBusy(true); setError(''); try { if (mode === 'register') await posApi.register(email, password); await login(email, password); } catch (exception) { setError(exception instanceof Error ? exception.message : 'Unable to continue.'); } finally { setBusy(false); } };
  return <main className="auth-page"><section className="auth-intro"><div className="brand-mark"><ShoppingBag size={25} /></div><p className="eyebrow">SELL SMARTER</p><h1>Everything your counter needs.</h1><p>Run fast, delightful checkouts and keep every product, sale, and team member in one clear workspace.</p><div className="intro-card"><CheckCircle2 size={18} /> Designed for the rhythm of real retail</div></section><section className="auth-panel"><div className="auth-card"><div><p className="eyebrow">WELCOME TO COUNTERLY</p><h2>{mode === 'login' ? 'Welcome back' : 'Set up your store'}</h2><p>{mode === 'login' ? 'Sign in to start selling.' : 'Create the first owner account for your store.'}</p></div><form onSubmit={submit}><label>Email address<input type="email" placeholder="you@store.com" value={email} onChange={(event) => setEmail(event.target.value)} required /></label><label>Password<input type="password" placeholder="Minimum 8 characters" value={password} onChange={(event) => setPassword(event.target.value)} required minLength={8} /></label>{error && <p className="form-error">{error}</p>}<button className="primary-button full" disabled={busy}>{busy ? 'Please wait…' : mode === 'login' ? 'Sign in to Counterly' : 'Create owner account'}</button></form><button className="text-button" onClick={() => { setMode(mode === 'login' ? 'register' : 'login'); setError(''); }}>{mode === 'login' ? 'New store? Create your owner account' : 'Already registered? Sign in'}</button></div></section></main>;
}

function AppShell() {
  const { session, logout } = useAuth();
  const isOwner = session?.role === 'Owner';
  const [view, setView] = useState<View>(isOwner ? 'dashboard' : 'checkout');
  const [products, setProducts] = useState<Product[]>([]); const [categories, setCategories] = useState<Category[]>([]); const [cart, setCart] = useState<CartLine[]>([]);
  const [sales, setSales] = useState<PagedResult<Sale> | null>(null); const [dailyRevenue, setDailyRevenue] = useState<DailyRevenue[]>([]); const [topProducts, setTopProducts] = useState<TopProduct[]>([]);
  const [loading, setLoading] = useState(false); const [notice, setNotice] = useState<Notice>(null); const [modal, setModal] = useState<'product' | 'category' | 'team' | null>(null); const [editingProduct, setEditingProduct] = useState<Product | null>(null); const [search, setSearch] = useState('');
  const showError = (exception: unknown) => setNotice({ message: exception instanceof Error ? exception.message : 'Something went wrong.', type: 'error' });
  const loadCatalog = async () => { const [catalog, groups] = await Promise.all([posApi.products(), isOwner ? posApi.categories() : Promise.resolve([])]); setProducts(catalog); setCategories(groups); };
  const loadView = async () => { setLoading(true); try { if (view === 'dashboard') { const [catalog, groups, revenue, top] = await Promise.all([posApi.products(), posApi.categories(), posApi.dailyRevenue(), posApi.topProducts()]); setProducts(catalog); setCategories(groups); setDailyRevenue(revenue); setTopProducts(top); } else if (view === 'sales') setSales(await posApi.sales()); else await loadCatalog(); } catch (exception) { showError(exception); } finally { setLoading(false); } };
  useEffect(() => { void loadView(); }, [view]);
  useEffect(() => { if (!notice) return; const timer = window.setTimeout(() => setNotice(null), 4000); return () => window.clearTimeout(timer); }, [notice]);
  const nav: { id: View; label: string; icon: typeof LayoutDashboard; owner?: boolean }[] = [{ id: 'dashboard', label: 'Overview', icon: LayoutDashboard, owner: true }, { id: 'checkout', label: 'New sale', icon: WalletCards }, { id: 'inventory', label: 'Products', icon: Boxes, owner: true }, { id: 'categories', label: 'Categories', icon: ClipboardList, owner: true }, { id: 'sales', label: 'Sales history', icon: ReceiptText }, { id: 'team', label: 'Team', icon: Users, owner: true }];
  const addToCart = (product: Product) => { if (product.stockQty < 1) return; setCart((current) => { const found = current.find((line) => line.id === product.id); return found ? current.map((line) => line.id === product.id ? { ...line, quantity: Math.min(line.quantity + 1, product.stockQty) } : line) : [...current, { ...product, quantity: 1 }]; }); };
  const changeQuantity = (id: string, quantity: number) => setCart((current) => quantity < 1 ? current.filter((line) => line.id !== id) : current.map((line) => line.id === id ? { ...line, quantity: Math.min(quantity, line.stockQty) } : line));
  const checkout = async (paymentMethod: 'Cash' | 'Card') => { try { const sale = await posApi.createSale(paymentMethod, cart.map(({ id, quantity }) => ({ productId: id, quantity }))); setCart([]); await loadCatalog(); setNotice({ message: `Sale completed · ${formatMoney(sale.totalAmount)}` }); } catch (exception) { showError(exception); } };
  const saveProduct = async (product: Omit<Product, 'id' | 'categoryName' | 'createdAt'>) => { try { await posApi.saveProduct(product, editingProduct?.id); setModal(null); setEditingProduct(null); await loadCatalog(); setNotice({ message: `Product ${editingProduct ? 'updated' : 'added'} successfully.` }); } catch (exception) { showError(exception); } };
  const saveCategory = async (name: string) => { try { await posApi.saveCategory(name); setModal(null); await loadCatalog(); setNotice({ message: 'Category created successfully.' }); } catch (exception) { showError(exception); } };
  const catalog = products.filter((product) => `${product.name} ${product.sku}`.toLowerCase().includes(search.toLowerCase()));
  const total = cart.reduce((sum, line) => sum + line.price * line.quantity, 0);
  return <div className="app-shell"><aside className="sidebar"><div className="sidebar-brand"><div className="brand-mark"><ShoppingBag size={20} /></div><span>counterly</span></div><nav>{nav.filter((item) => !item.owner || isOwner).map((item) => { const Icon = item.icon; return <button key={item.id} className={view === item.id ? 'nav-item active' : 'nav-item'} onClick={() => setView(item.id)}><Icon size={19} />{item.label}</button>; })}</nav><div className="sidebar-user"><div className="avatar">{session?.email[0].toUpperCase()}</div><div><strong>{session?.email.split('@')[0]}</strong><span>{session?.role}</span></div><button className="icon-button" title="Sign out" onClick={logout}><LogOut size={18} /></button></div></aside><main className="content"><header className="topbar"><div><p className="eyebrow">{view === 'checkout' ? 'POINT OF SALE' : 'STORE OPERATIONS'}</p><h1>{({ dashboard: 'Good morning', checkout: 'New sale', inventory: 'Product catalog', categories: 'Categories', sales: 'Sales history', team: 'Your team' } as Record<View, string>)[view]}</h1></div>{view === 'inventory' && <button className="primary-button" onClick={() => { setEditingProduct(null); setModal('product'); }}><PackagePlus size={18} /> Add product</button>}{view === 'categories' && <button className="primary-button" onClick={() => setModal('category')}><Plus size={18} /> New category</button>}{view === 'team' && <button className="primary-button" onClick={() => setModal('team')}><Plus size={18} /> Invite teammate</button>}</header>{loading ? <div className="loading">Loading your workspace…</div> : <>{view === 'dashboard' && <Dashboard products={products} categories={categories} revenue={dailyRevenue} top={topProducts} onStartSale={() => setView('checkout')} />}{view === 'checkout' && <Checkout products={catalog} cart={cart} search={search} total={total} onSearch={setSearch} onAdd={addToCart} onQuantity={changeQuantity} onCheckout={checkout} />}{view === 'inventory' && <Inventory products={catalog} search={search} onSearch={setSearch} onEdit={(product) => { setEditingProduct(product); setModal('product'); }} onDelete={async (id) => { if (window.confirm('Delete this product?')) try { await posApi.deleteProduct(id); await loadCatalog(); setNotice({ message: 'Product deleted.' }); } catch (exception) { showError(exception); } }} />}{view === 'categories' && <Categories categories={categories} onDelete={async (id) => { if (window.confirm('Delete this category?')) try { await posApi.deleteCategory(id); await loadCatalog(); setNotice({ message: 'Category deleted.' }); } catch (exception) { showError(exception); } }} />}{view === 'sales' && <SalesHistory sales={sales} onPage={async (page) => { try { setLoading(true); setSales(await posApi.sales(page)); } catch (exception) { showError(exception); } finally { setLoading(false); } }} />}{view === 'team' && <Team />}</>}</main>{modal === 'product' && <ProductModal categories={categories} product={editingProduct} onSave={saveProduct} onClose={() => setModal(null)} />}{modal === 'category' && <SimpleModal title="New category" label="Category name" action="Create category" onSubmit={saveCategory} onClose={() => setModal(null)} />}{modal === 'team' && <TeamModal onClose={() => setModal(null)} onSuccess={(message) => { setModal(null); setNotice({ message }); }} onError={showError} />}{notice && <Toast {...notice} />}</div>;
}

function Dashboard({ products, categories, revenue, top, onStartSale }: { products: Product[]; categories: Category[]; revenue: DailyRevenue[]; top: TopProduct[]; onStartSale: () => void }) { const today = new Date().toISOString().slice(0, 10); const todayRevenue = revenue.find((item) => item.date.slice(0, 10) === today); const totalRevenue = revenue.reduce((sum, item) => sum + item.totalRevenue, 0); const lowStock = products.filter((item) => item.stockQty < 6).length; return <><section className="stats"><Stat icon={<WalletCards />} label="Today’s sales" value={formatMoney(todayRevenue?.totalRevenue ?? 0)} detail={`${todayRevenue?.saleCount ?? 0} transactions`} tone="purple" /><Stat icon={<BarChart3 />} label="Total revenue" value={formatMoney(totalRevenue)} detail="All recorded sales" tone="orange" /><Stat icon={<Boxes />} label="Products in stock" value={String(products.length)} detail={`${lowStock} running low`} tone="blue" /><Stat icon={<ClipboardList />} label="Categories" value={String(categories.length)} detail="Organize your catalog" tone="green" /></section><section className="dashboard-grid"><article className="panel revenue-panel"><div className="panel-heading"><div><h2>Revenue pulse</h2><p>Daily sales activity</p></div><span className="pill">Live data</span></div>{revenue.length ? <div className="chart">{revenue.slice(-10).map((item) => <div className="bar-group" key={item.date}><div className="bar" style={{ height: `${Math.max(12, (item.totalRevenue / Math.max(...revenue.map((row) => row.totalRevenue))) * 155)}px` }} title={formatMoney(item.totalRevenue)} /><small>{new Date(item.date).toLocaleDateString('en-IN', { day: 'numeric', month: 'short' })}</small></div>)}</div> : <EmptyState icon={<BarChart3 />} title="No revenue yet" detail="Completed sales will appear here." />}</article><article className="panel"><div className="panel-heading"><div><h2>Best sellers</h2><p>Your top-performing products</p></div></div>{top.length ? <div className="rank-list">{top.slice(0, 5).map((item, index) => <div className="rank-row" key={item.productId}><span className="rank">0{index + 1}</span><div><strong>{item.productName}</strong><small>{item.quantitySold} units sold</small></div><b>{formatMoney(item.revenue)}</b></div>)}</div> : <EmptyState icon={<ShoppingBag />} title="Your counter is ready" detail="Make your first sale to see best sellers." />}</article></section><section className="callout"><div><span className="callout-icon"><CreditCard size={22} /></span><div><h2>Ready to make a sale?</h2><p>Open a fresh checkout and keep your line moving.</p></div></div><button className="light-button" onClick={onStartSale}>Open checkout <ChevronRight size={17} /></button></section></> }
function Stat({ icon, label, value, detail, tone }: { icon: ReactNode; label: string; value: string; detail: string; tone: string }) { return <article className="stat"><span className={`stat-icon ${tone}`}>{icon}</span><p>{label}</p><h2>{value}</h2><small>{detail}</small></article>; }
function Checkout({ products, cart, search, total, onSearch, onAdd, onQuantity, onCheckout }: { products: Product[]; cart: CartLine[]; search: string; total: number; onSearch: (value: string) => void; onAdd: (product: Product) => void; onQuantity: (id: string, quantity: number) => void; onCheckout: (method: 'Cash' | 'Card') => Promise<void> }) { const [paying, setPaying] = useState(false); const pay = async (method: 'Cash' | 'Card') => { if (!cart.length) return; setPaying(true); await onCheckout(method); setPaying(false); }; return <div className="checkout-layout"><section><div className="search-field"><Search size={19} /><input value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Search products or SKU…" /></div><div className="product-grid">{products.map((product) => <button className="product-card" key={product.id} disabled={product.stockQty < 1} onClick={() => onAdd(product)}><div className="product-image"><ShoppingBag size={25} /></div><div><span>{product.categoryName ?? 'Uncategorized'}</span><h3>{product.name}</h3><p>{formatMoney(product.price)}</p></div><small className={product.stockQty < 6 ? 'low-stock' : ''}>{product.stockQty ? `${product.stockQty} left` : 'Out of stock'}</small></button>)}</div>{!products.length && <EmptyState icon={<Search />} title="No matching products" detail="Try a different search or add inventory first." />}</section><aside className="cart"><div className="cart-heading"><div><h2>Current order</h2><p>{cart.length ? `${cart.length} item${cart.length === 1 ? '' : 's'} in cart` : 'Add products to get started'}</p></div><ReceiptText size={21} /></div><div className="cart-lines">{cart.length ? cart.map((line) => <div className="cart-line" key={line.id}><div className="line-product"><strong>{line.name}</strong><small>{formatMoney(line.price)} each</small></div><div className="quantity"><button onClick={() => onQuantity(line.id, line.quantity - 1)}>−</button><span>{line.quantity}</span><button onClick={() => onQuantity(line.id, line.quantity + 1)}>+</button></div><b>{formatMoney(line.price * line.quantity)}</b></div>) : <EmptyState icon={<ShoppingBag />} title="Your cart is empty" detail="Choose products from the catalog." />}</div><div className="cart-total"><span>Total</span><strong>{formatMoney(total)}</strong></div><div className="payment-buttons"><button disabled={!cart.length || paying} onClick={() => void pay('Cash')}><WalletCards size={18} /> Cash</button><button disabled={!cart.length || paying} onClick={() => void pay('Card')}><CreditCard size={18} /> Card</button></div></aside></div>; }
function Inventory({ products, search, onSearch, onEdit, onDelete }: { products: Product[]; search: string; onSearch: (value: string) => void; onEdit: (product: Product) => void; onDelete: (id: string) => Promise<void> }) { return <section className="panel table-panel"><div className="table-toolbar"><div className="search-field compact"><Search size={18} /><input value={search} onChange={(event) => onSearch(event.target.value)} placeholder="Search products…" /></div><span>{products.length} products</span></div>{products.length ? <div className="table-wrap"><table><thead><tr><th>Product</th><th>SKU</th><th>Category</th><th>Price</th><th>Stock</th><th></th></tr></thead><tbody>{products.map((product) => <tr key={product.id}><td><strong>{product.name}</strong></td><td>{product.sku}</td><td>{product.categoryName ?? '—'}</td><td>{formatMoney(product.price)}</td><td><span className={`stock-badge ${product.stockQty < 6 ? 'warning' : ''}`}>{product.stockQty} units</span></td><td className="actions"><button onClick={() => onEdit(product)}>Edit</button><button className="danger" onClick={() => void onDelete(product.id)}><Trash2 size={16} /></button></td></tr>)}</tbody></table></div> : <EmptyState icon={<Boxes />} title="No products yet" detail="Add your first product to start selling." />}</section>; }
function Categories({ categories, onDelete }: { categories: Category[]; onDelete: (id: string) => Promise<void> }) { return <section className="category-grid">{categories.map((category) => <article className="category-card" key={category.id}><div className="category-icon"><ClipboardList size={20} /></div><strong>{category.name}</strong><button className="icon-button danger" onClick={() => void onDelete(category.id)}><Trash2 size={17} /></button></article>)}{!categories.length && <EmptyState icon={<ClipboardList />} title="No categories yet" detail="Create categories to keep your catalog tidy." />}</section>; }
function SalesHistory({ sales, onPage }: { sales: PagedResult<Sale> | null; onPage: (page: number) => Promise<void> }) { if (!sales?.items.length) return <EmptyState icon={<ReceiptText />} title="No sales recorded" detail="Completed checkouts will show up here." />; return <section className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>Receipt</th><th>Cashier</th><th>Payment</th><th>Items</th><th>Created</th><th>Total</th></tr></thead><tbody>{sales.items.map((sale) => <tr key={sale.id}><td><strong>#{sale.id.slice(0, 8).toUpperCase()}</strong></td><td>{sale.cashierEmail}</td><td><span className="pill">{sale.paymentMethod}</span></td><td>{sale.items.reduce((sum, item) => sum + item.quantity, 0)}</td><td>{formatDate(sale.createdAt)}</td><td><strong>{formatMoney(sale.totalAmount)}</strong></td></tr>)}</tbody></table></div><div className="pagination"><span>Page {sales.page} of {Math.max(1, Math.ceil(sales.totalCount / sales.pageSize))}</span><div><button className="icon-button" disabled={sales.page <= 1} onClick={() => void onPage(sales.page - 1)}><ChevronLeft size={18} /></button><button className="icon-button" disabled={sales.page * sales.pageSize >= sales.totalCount} onClick={() => void onPage(sales.page + 1)}><ChevronRight size={18} /></button></div></div></section>; }
function Team() { return <section className="team-card"><div className="team-illustration"><Users size={34} /></div><div><p className="eyebrow">STAFF ACCESS</p><h2>Build a confident counter team</h2><p>Invite cashiers from the button above. They can make sales and view their own sales history, while your catalog and reports remain protected.</p></div></section>; }
function ProductModal({ categories, product, onSave, onClose }: { categories: Category[]; product: Product | null; onSave: (product: Omit<Product, 'id' | 'categoryName' | 'createdAt'>) => Promise<void>; onClose: () => void }) { const [name, setName] = useState(product?.name ?? ''); const [sku, setSku] = useState(product?.sku ?? ''); const [price, setPrice] = useState(String(product?.price ?? '')); const [stockQty, setStockQty] = useState(String(product?.stockQty ?? 0)); const [categoryId, setCategoryId] = useState(product?.categoryId ?? ''); const [busy, setBusy] = useState(false); const submit = async (event: FormEvent) => { event.preventDefault(); setBusy(true); await onSave({ name, sku, price: Number(price), stockQty: Number(stockQty), categoryId: categoryId || null }); setBusy(false); }; return <Modal title={product ? 'Edit product' : 'Add product'} onClose={onClose}><form className="modal-form" onSubmit={submit}><label>Product name<input value={name} onChange={(event) => setName(event.target.value)} required /></label><div className="form-row"><label>SKU<input value={sku} onChange={(event) => setSku(event.target.value)} required /></label><label>Category<select value={categoryId} onChange={(event) => setCategoryId(event.target.value)}><option value="">Uncategorized</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label></div><div className="form-row"><label>Price<input type="number" min="0.01" step="0.01" value={price} onChange={(event) => setPrice(event.target.value)} required /></label><label>Stock quantity<input type="number" min="0" value={stockQty} onChange={(event) => setStockQty(event.target.value)} required /></label></div><button className="primary-button full" disabled={busy}>{busy ? 'Saving…' : 'Save product'}</button></form></Modal>; }
function SimpleModal({ title, label, action, onSubmit, onClose }: { title: string; label: string; action: string; onSubmit: (value: string) => Promise<void>; onClose: () => void }) { const [value, setValue] = useState(''); return <Modal title={title} onClose={onClose}><form className="modal-form" onSubmit={async (event) => { event.preventDefault(); await onSubmit(value); }}><label>{label}<input value={value} onChange={(event) => setValue(event.target.value)} required autoFocus /></label><button className="primary-button full">{action}</button></form></Modal>; }
function TeamModal({ onClose, onSuccess, onError }: { onClose: () => void; onSuccess: (message: string) => void; onError: (exception: unknown) => void }) { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [role, setRole] = useState<'Cashier' | 'Owner'>('Cashier'); const [busy, setBusy] = useState(false); return <Modal title="Invite teammate" onClose={onClose}><form className="modal-form" onSubmit={async (event) => { event.preventDefault(); setBusy(true); try { await posApi.createUser(email, password, role === 'Owner'); onSuccess(`${role} account created successfully.`); } catch (exception) { onError(exception); } finally { setBusy(false); } }}><label>Email address<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} required /></label><label>Access level<select value={role} onChange={(event) => setRole(event.target.value as 'Cashier' | 'Owner')}><option value="Cashier">Cashier — checkout and own sales</option><option value="Owner">Owner — full store access</option></select></label><label>Temporary password<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} required minLength={8} /></label><button className="primary-button full" disabled={busy}>{busy ? 'Creating…' : `Create ${role.toLowerCase()}`}</button></form></Modal>; }
export default function App() { const { session } = useAuth(); return session ? <AppShell /> : <LoginScreen />; }
